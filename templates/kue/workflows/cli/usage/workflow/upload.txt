Upload a workflow with its action dependencies (POST /workflow)

Usage:
  kue workflow upload <name> [options]

Options:
  --name, -n      Workflow name (alternative to positional arg)
  --host, -H      API host (default: api.kuetix.com)
  --config, -c    Path to config file (default: ~/.kue/config.json)
