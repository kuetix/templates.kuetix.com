Delete a workflow on the server (DELETE /workflow/{name})

Usage:
  kue workflow delete <name> [options]

Options:
  --name, -n      Workflow name (alternative to positional arg)
  --host, -H      API host (default: api.kuetix.com)
  --config, -c    Path to config file
